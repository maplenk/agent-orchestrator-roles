// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: session_failover.sql

package gen

import (
	"context"
	"time"
)

const insertSessionFailoverAttempt = `-- name: InsertSessionFailoverAttempt :exec

INSERT INTO session_failover_attempts (
    id, session_id, project_id, incident_id, seq,
    role_id, from_harness, from_model, to_harness, to_model,
    rung_index, generation_id, state, created_at, updated_at, source_generation_id
) VALUES (
    ?, ?, ?, ?, ?,
    ?, ?, ?, ?, ?,
    ?, ?, ?, ?, ?, ?
)
`

type InsertSessionFailoverAttemptParams struct {
	ID                 string
	SessionID          string
	ProjectID          string
	IncidentID         string
	Seq                int64
	RoleID             string
	FromHarness        string
	FromModel          string
	ToHarness          string
	ToModel            string
	RungIndex          int64
	GenerationID       string
	State              string
	CreatedAt          time.Time
	UpdatedAt          time.Time
	SourceGenerationID string
}

// ASCII ONLY IN THIS FILE. This is not a style preference; see the NOTE at the
// bottom. A single multi-byte character anywhere above a statement silently
// truncates that statement's generated SQL.
// Appends one continuation attempt. Written inside the SAME transaction as the
// failover/requested lifecycle ledger row (contract section 6 rule 1) -- see
// Store.AppendSessionFailoverAttemptWithLedger. The
// UNIQUE(session_id, incident_id, seq) constraint is the last-resort idempotence
// fence behind that transaction: a retry that recomputed the same seq fails
// here, and because the ledger row is in the same transaction it is rolled back
// with it rather than left behind as an orphan.
//
// generation_id is supplied by the caller and is never empty: Continue mints the
// switch generation before this insert and pins it into the saga via
// SwitchRequest.ForceGenerationID (contract section 6b).
func (q *Queries) InsertSessionFailoverAttempt(ctx context.Context, arg InsertSessionFailoverAttemptParams) error {
	_, err := q.db.ExecContext(ctx, insertSessionFailoverAttempt,
		arg.ID,
		arg.SessionID,
		arg.ProjectID,
		arg.IncidentID,
		arg.Seq,
		arg.RoleID,
		arg.FromHarness,
		arg.FromModel,
		arg.ToHarness,
		arg.ToModel,
		arg.RungIndex,
		arg.GenerationID,
		arg.State,
		arg.CreatedAt,
		arg.UpdatedAt,
		arg.SourceGenerationID,
	)
	return err
}

const listSessionFailoverAttemptsByIncident = `-- name: ListSessionFailoverAttemptsByIncident :many
SELECT
    id, session_id, project_id, incident_id, seq,
    role_id, from_harness, from_model, to_harness, to_model,
    rung_index, generation_id, state, created_at, updated_at, source_generation_id
FROM session_failover_attempts
WHERE session_id = ? AND incident_id = ?
ORDER BY seq ASC
`

type ListSessionFailoverAttemptsByIncidentParams struct {
	SessionID  string
	IncidentID string
}

// Every attempt for one incident, oldest first. `used` in
// domain.NextFailoverRung is built from ALL of these regardless of state: a
// rung that FAILED is spent, not retried. The last row is the latest attempt,
// which is the one idempotence adopts when it is non-terminal (contract 6a).
func (q *Queries) ListSessionFailoverAttemptsByIncident(ctx context.Context, arg ListSessionFailoverAttemptsByIncidentParams) ([]SessionFailoverAttempt, error) {
	rows, err := q.db.QueryContext(ctx, listSessionFailoverAttemptsByIncident, arg.SessionID, arg.IncidentID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []SessionFailoverAttempt{}
	for rows.Next() {
		var i SessionFailoverAttempt
		if err := rows.Scan(
			&i.ID,
			&i.SessionID,
			&i.ProjectID,
			&i.IncidentID,
			&i.Seq,
			&i.RoleID,
			&i.FromHarness,
			&i.FromModel,
			&i.ToHarness,
			&i.ToModel,
			&i.RungIndex,
			&i.GenerationID,
			&i.State,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.SourceGenerationID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listSessionFailoverAttemptsBySession = `-- name: ListSessionFailoverAttemptsBySession :many
SELECT
    id, session_id, project_id, incident_id, seq,
    role_id, from_harness, from_model, to_harness, to_model,
    rung_index, generation_id, state, created_at, updated_at, source_generation_id
FROM session_failover_attempts
WHERE session_id = ?
ORDER BY created_at ASC, seq ASC
`

// Every attempt for a session across incidents, oldest first. Reconciliation
// reads this rather than one incident's slice: an attempt left non-terminal by
// a crash belongs to whichever incident was live at the time, and boot has no
// incident in hand to ask about.
func (q *Queries) ListSessionFailoverAttemptsBySession(ctx context.Context, sessionID string) ([]SessionFailoverAttempt, error) {
	rows, err := q.db.QueryContext(ctx, listSessionFailoverAttemptsBySession, sessionID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []SessionFailoverAttempt{}
	for rows.Next() {
		var i SessionFailoverAttempt
		if err := rows.Scan(
			&i.ID,
			&i.SessionID,
			&i.ProjectID,
			&i.IncidentID,
			&i.Seq,
			&i.RoleID,
			&i.FromHarness,
			&i.FromModel,
			&i.ToHarness,
			&i.ToModel,
			&i.RungIndex,
			&i.GenerationID,
			&i.State,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.SourceGenerationID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateSessionFailoverAttemptState = `-- name: UpdateSessionFailoverAttemptState :execrows
UPDATE session_failover_attempts
SET state = ?, updated_at = ?
WHERE id = ? AND state = ?
`

type UpdateSessionFailoverAttemptStateParams struct {
	State     string
	UpdatedAt time.Time
	ID        string
	State_2   string
}

// Moves an attempt to a new state under a compare-and-set on the state the
// caller observed. The generation is NOT written here: it is durable from the
// very first insert (contract section 6b), so there is no stamp step and
// therefore no query that can rewrite a generation -- which is what made crash
// recovery a guess in the first draft.
//
// The CAS is what makes the two completers of a post_stop safe against each
// other: an operator Continue and boot's post_stop recovery both try
// post_stop -> acked, and the loser gets 0 rows. execrows: 0 means the guard
// rejected the write, which is an answer, not an error.
func (q *Queries) UpdateSessionFailoverAttemptState(ctx context.Context, arg UpdateSessionFailoverAttemptStateParams) (int64, error) {
	result, err := q.db.ExecContext(ctx, updateSessionFailoverAttemptState,
		arg.State,
		arg.UpdatedAt,
		arg.ID,
		arg.State_2,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}
